#include <stdio.h>

int main(){

#define square(x)	(x)*(x)
	

	int num[3] = {0,1,2};
	int sum = 1000;

	while(1 == 1){

		for(int i = 1; 

		if(num[1] >= 32){
			num[0] ++;
			num[1] = num[0] + 1;
		}

		num[1]++;
	}
	
	printf("%d %d %d\n", num[0], num[1], num[2]);
	printf("%d\n", num[0] * num[1] * num[2]);

	return 0;
}


